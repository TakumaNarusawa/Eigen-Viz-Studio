# Eigen-Viz Studio リリースノート

Eigen-Viz Studio のこれまでのリリース履歴と主な変更点です。

## 📝 リリース履歴

### v1.4.0 (2026-05-30)
本バージョンでは、システム全体の数学的厳密化、パフォーマンスの極限最適化、GPUメモリ管理の堅牢化、および操作性を高める極上のホバーインタラクションと高解像度対応が導入されました。

- **「使い方ガイド」モーダルの実装（完全手動起動のUX重視設計）**:
  - デモサイトから訪れたユーザー向けに、アプリの幾何学的概念と操作方法を簡潔に紹介する「使い方ガイド」ボタンをヘッダーに配置。
  - 起動時（初回アクセス含む）の自動ポップアップを廃止し、ユーザーが自分の意志で確認できる**完全手動起動（ヘッダーボタンクリック時のみ表示）**に統一してUXを向上。
  - **出現演出・スクロール性・操作性の徹底強化**:
    - モーダルの出現時に滑らかなスケール＆フェードイン効果（`scale(0.9) translateY(20px)` からの滑らかな Cubic-Bezier イージング変化）を導入し、高級感のある出現演出を実現。
    - コンテンツ領域の高さをデバイスサイズに応じて制限し、どのような解像度のディスプレイであっても確実に見切れた文字列がスクロールできるよう、スクロールコンテインメント（スクロール追従性）を担保。
    - 閉じる（×）ボタンを拡大（`40px`）し、円形のネオンデザインへと刷新。ホバー時に `90度回転` しつつ赤色のネオン発光・シャドウを伴う直感的な視覚フィードバックを追加。
  - **全ファイルの徹底的なコード精査と品質の極大化**:
    - 2Dモードにおいて、複素固有空間（不変楕円）の解析結果数式にマウスホバーした際、データ属性が未付与のためNull参照エラー（TypeError）を吐く潜在的なバグを発見し、完全修正。
    - モーダルを閉じる際のJSタイマー遅延を `300ms` からCSSのフェード時間と完全に一致する `400ms` へと調整し、ポップアップが消えるアニメーションが途中で途切れることなく最も美しく描画されるように同期。
- **数式とグラフィックスのホバー連動ハイライト**:
  - 計算結果パネルのKaTeX数式ホバー時に、対応する2D/3D上の固有空間の軸（および2D複素不変楕円の軌道）が強くネオン発光し、それ以外の軸が薄暗く減光する極上インタラクションを追加。
- **高解像度ディスプレイ（DPI/Retina）対応**:
  - `devicePixelRatio` に基づいて Canvas の物理ピクセル解像度を自動スケールし、高画素密度環境下で極めてシャープなベクトル描画を実現。
- **数学的厳密化と数値演算ノイズの極限除去**:
  - 対角化可能判定にSymPyの公式メソッド `M.is_diagonalizable()` を導入し、数学的に完璧な整合性を保証。
  - $10^{-10}$ 未満の微小な虚数ノイズを自動で丸め込む `clean_complex()` を実装し、不要な複素数判定の誘発や描画のブレを完全に防止。
  - 複素固有値・ベクトルの実部・虚部を完璧に保持した `eigenvalues_complex` / `eigenvectors_complex` フィールドを新設し、将来の拡張性を確保。
- **WebGL描画のリアクティブ最適化 & メモリリークの完全解消**:
  - `lastMatrix` を用いた状態変更検知（Dirtyフラグ制御）を導入し、アニメーション（行列変化）中のみ頂点バッファ転送（`position.needsUpdate = true`）を行うリアクティブ設計に刷新。待機中のCPU/GPU負荷を実質ゼロに低減。
  - Three.jsオブジェクト削除時に geometry と material を明示的にGPUメモリから破棄する `clearGroup()` ヘルパーを実装し、メモリリークを完全に解消。
  - 2D/3Dのレンダーループ内で毎フレーム定義されていた行列計算用のローカル関数を外部ヘルパーへと排出し、GC（ガベージコレクション）によるカクつきを排除。
  - 非アクティブなモードの `requestAnimationFrame` を完全に停止する制御（start/stopインターフェース）を実装し、省電力性を極限まで強化。
  - 対角化アニメーション（P⁻¹, D, Pの3段階変形）の各遷移に対して個別に Cubic Ease Out イージングを適用し、滑らかなアニメーション遷移を実現。

### v1.3.0 (2026-05-28)
本バージョンは、Google DeepMindが開発したAIコーディングアシスタント **Antigravity** との共同開発により、堅牢性の向上とリファクタリングが実施されました。

- **セキュリティの強化**: `worker.js`におけるPythonインジェクション脆弱性（CWE-94）の修正、ユーザー入力エスケープによるXSS対策。
- **UI/UX・視覚的差別化の向上**:
  - 基底ベクトルと固有ベクトルの視覚的差別化（パルス明滅、3D破線シアン化、2Dラベル追加）。
  - WebWorkerのタイムアウト検知と自動復帰ロジック（スレッド再生成）の実装による計算プロセスの堅牢化。
- **ドキュメントの強化**: READMEの構成変更と、本リリースノート（`RELEASE_NOTES.md`）への分割・整理。

### v1.2.0 (2026-05-28)
- **UIの洗練**: 操作ボタンのレイアウトを横並びに最適化。
- **HUDの改善**: 計算結果HUDを右下へ移動、不要な進捗バーを削除してUIを整理。

### v1.1.0 (2026-05-28)
- **デプロイ設定の追加**: Cloudflare Pagesデプロイ用の `wrangler.jsonc` 設定の追加。
- **デモサイト公開**: READMEへのデモサイト公開URL of 追加。

### v1.0.0 (2026-05-28)
- **初回リリース**:
  - WebGL (Three.js) および HTML5 Canvas による固有値・固有ベクトルの極上2D/3D可視化。
  - Pyodide / SymPy を採用したブラウザ完結型の高精度な数式計算エンジン。
  - ハーモニアス・ダークネオン & グラスモフィズムUIデザインの適用。
